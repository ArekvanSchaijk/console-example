options:
  ssl_port: 443
  force_https: true

server:
  host: 'connect.development.alternet.nl'
  port: 22
  username: 'alternetcli'
  document_root: '/home/alternetcli/{environment.server_name}'
  post_build:
    - 'sudo systemctl restart httpd'

database:
  default:
    username: 'root'
    password: 'e.<0$I:/NQ4(.Jf'
    port: 3306